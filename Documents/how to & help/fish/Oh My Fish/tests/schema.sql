#!/bin/sh

# Environment Variables
# ---
# You can use the `source` command to add these variables to your
# terminal environment, like so:
# $ source bin/env.sh

export FLASK_APP=flasktodo
export FLASK_ENV=development
